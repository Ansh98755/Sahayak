<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sahayak">
    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sahayak"
        tools:targetApi="31">
        <activity
            android:name=".StaffActivity"
            android:exported="true"
            android:label="@string/title_activity_staff"
            android:theme="@style/Theme.Sahayak">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Booktest"
            android:exported="true"
            android:label="@string/title_activity_booktest"
            android:theme="@style/Theme.Sahayak"></activity> <!-- Ensure this points to your Application class -->
        <!-- SplashActivity as launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="@string/title_activity_splash"
            android:theme="@style/Theme.Sahayak">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Other activities -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Sahayak" />
        <activity
            android:name=".SigninActivity"
            android:exported="true"
            android:label="@string/title_activity_signin"
            android:theme="@style/Theme.Sahayak" />
        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:label="@string/title_activity_signup"
            android:theme="@style/Theme.Sahayak" />
        <activity
            android:name=".IntermediateActivity"
            android:exported="true"
            android:label="@string/title_activity_intermediate"
            android:theme="@style/Theme.Sahayak" />
        <activity
            android:name=".SigninActivitySecond"
            android:exported="true"
            android:label="@string/title_activity_signin_second"
            android:theme="@style/Theme.Sahayak" />
    </application>

</manifest>